FROM 192.168.99.100:5000/icycrystal4/wb-nginx:0.6

MAINTAINER xiaokai.wang xiaokai@staff

RUN mkdir -p /data/logs

ADD discover.nginx.conf /data/discover.nginx.conf
ADD mime.types /data/mime.types

WORKDIR /data

CMD "/usr/local/nginx/sbin/nginx" "-c" "/data/discover.nginx.conf"